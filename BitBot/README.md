Just changes a typical unicode import error "Mu nchen" to "München" in publish place in editions.

## Secrets

Create `secrets.py` in the following format:

        secrets = {
                'username': 'username',
                'password': 'password'
        }
